{
  "name": "j13z-svelte-agent-skills",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "pnpm run typecheck && tsx scripts/build.ts build",
    "clean": "pnpm run typecheck && tsx scripts/build.ts clean",
    "install:local": "pnpm run clean && pnpm run build && tsx scripts/build.ts install-local"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
